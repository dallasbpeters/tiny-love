extends ../../layouts/default

block content
	.container: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
		article
			hr
			if !data.page
				h2 Invalid Post.
			else
				header
					h1 asdfasdfasd
					h1= data.page.title
				.page
					if data.page.image.exists
						.image-wrap: img(src=data.page._.image.fit(750,450)).img-responsive
					!= data.page.content
